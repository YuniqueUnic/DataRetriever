<UserControl x:Class="VSTSDataProvider.Views.LeftAlternativeItemSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:local="clr-namespace:VSTSDataProvider.Views"
             xmlns:prop="clr-namespace:VSTSDataProvider.Properties.Language"
             d:Background="White"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <DockPanel Name="TopFunctionDockPanel">
            <StackPanel Margin="4,2"
                        VerticalAlignment="Center"
                        DockPanel.Dock="Top"
                        Orientation="Horizontal">
                <Button Content="👁" />
            </StackPanel>
            <Grid>
                <ListView d:ItemsSource="{d:DesignData IsDesignTimeCreatable=True, Source={d:SampleData ItemCount=100}}" />
            </Grid>
        </DockPanel>
    </Grid>
</UserControl>
